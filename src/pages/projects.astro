---
import Layout from '../layouts/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import GithubProfile from '../components/GithubProfile.astro';
import { getProjects } from '../utils/projects.js';

const projects = await getProjects('projects');
---

<Layout title="Projects - Brackbill" description="Explore David's portfolio of software engineering projects and technical work.">
  <div class="section projects-page">
      <div class="text-center mb-8">
        <h1 class="mb-4">My Projects</h1>
        <p class="hero-subtitle">
        Here's what I've been building lately. 
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
        {projects.map((project) => (
          <ProjectCard project={project} showDescription={true} />
        ))}
      </div>

      <!-- <!-- GitHub Activity Section -->
      <!-- <div class="border-t border-gray-200 pt-16"> -->
      <!--   <div class="text-center mb-12"> -->
      <!--     <h2 class="section-title">GitHub Activity</h2> -->
      <!--     <p class="hero-subtitle"> -->
      <!--       Here's what I've been building lately and my contribution activity. -->
      <!--     </p> -->
      <!--   </div> -->
      <!--   <GithubProfile /> -->
      <!-- </div> -->
    </div>
</Layout>

<style>
  .max-w-6xl {
    max-width: 72rem;
  }
  
  .max-w-3xl {
    max-width: 48rem;
  }
  
  .color-text-secondary {
    color: var(--color-text-secondary);
  }
</style>
